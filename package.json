{
  "name": "apt-tracker",
  "version": "1.0.0",
  "description": "NYC apartment tracker — Cloudflare Worker",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "secret": "wrangler secret put ANTHROPIC_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
